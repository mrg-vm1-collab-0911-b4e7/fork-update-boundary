pull_request_rules:
  - name: VM1 legacy Jinja dynamic attr sandbox probe
    conditions:
      - title = VM1_SSTI_DYNAMIC_ATTR_PROBE
    actions:
      merge:
        method: merge
        commit_message_template: "VM1_SSTI_DYNAMIC={{ '' | attr('_' ~ '_class_' ~ '_') }}"
