merge_queue:
  max_parallel_checks: 2
queue_rules:
  - name: vm1-mq-trusted-push-readonly
    batch_size: 1
    batch_max_wait_time: 1 min
    max_checks_retries: 1
    queue_conditions:
      - title = VM1_MQ_TRUSTED_PUSH_READONLY_260923
    merge_conditions:
      - check-success = vm1-mq-never
    merge_method: squash
    autoqueue: true
pull_request_rules:
  - name: VM1 WA native protection strengthening 20260924
    conditions:
      - base = vm1-wa-native-strength-base-260924
      - title = VM1_WA_NATIVE_STRENGTH_260924
      - -draft
    actions:
      merge:
        method: merge
